local utils = require("config.utils")
local group = utils.get_plugin_group()

if utils.enabled(group, "lazydev") then
  -- Configuration handled by lazy.nvim opts in lazy.lua
  -- This file exists for potential future customization
end
